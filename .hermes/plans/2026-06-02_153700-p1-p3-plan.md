# P1-P3 后续完善计划

**日期**: 2026-06-02
**状态**: 已完成 P0（5企业功能+123测试），进入 P1-P3

---

## 目标

从当前 ~78% 完整度推进到 ~88%+，覆盖清理-测试-组件三层。

---

## P1: 清理僵尸代码 (预计 10min)

### 背景
N8N 已完全移除（docker-compose/npm/模块注册），但磁盘残留 6 个文件。

### 步骤
1. 删除 `apps/api/src/modules/n8n-webhook/` (3文件)
   - n8n-webhook.controller.ts
   - n8n-webhook.module.ts
   - workflow-internal.controller.ts
2. 删除 `apps/api/src/integrations/n8n/` (3文件)
   - n8n.client.ts
   - n8n.client.spec.ts
   - n8n.module.ts
3. 检查 `app.module.ts` 确认无残留引用
4. `npx tsc --noEmit` 确认编译通过

### 风险
- 极低。上述模块无任何 import 引用（已在前序会话清理）

---

## P2: 新增页面 E2E 测试 (预计 30min)

### 背景
目前 5 个 Playwright spec 只覆盖 dashboard/new-project/demo/evaluation/auth。
5 个新页面零 E2E。

### 需新增

| Spec | 页面 | 关键验证点 |
|------|------|-----------|
| `project-spec.spec.ts` | /projects/[id]/spec | 生成→编辑→冻结→进入开发 完整流程 |
| `project-delivery.spec.ts` | /projects/[id]/delivery | 状态卡片/产物卡片/开始交付/进度条 |
| `project-deploy.spec.ts` | /projects/[id]/deploy | 部署状态/进度条/健康检查 |
| `project-idea.spec.ts` | /projects/[id]/idea | 10阶段访谈流程/跳过/完成 |

### 步骤
1. 确认后端运行 + 前端运行
2. 创建 `auth.setup.ts` 复用现有登录态
3. 编写 4 个 spec，每个 5-8 个 case
4. `npx playwright test` 验证

### 风险
- 需要 Docker 后端 + 前端同时运行
- delivery/deploy 页依赖真实交付数据 → 可 mock API 或使用已有项目

---

## P3: 前端组件抽取 (预计 20min)

### 背景
交付页/评估页内联逻辑过于臃肿，需抽取为独立组件。

### 需抽取

| 组件 | 来源 | 用途 |
|------|------|------|
| `delivery-status-card.tsx` | delivery/page.tsx | 三态卡片（未开始/进行中/已完成） |
| `delivery-progress-bar.tsx` | delivery/page.tsx | 终端风格进度条+4步流水线 |
| `coverage-chart.tsx` | evaluation/page.tsx | 覆盖率饼图/进度环 |

### 步骤
1. 从现有页面提取 JSX + 状态逻辑
2. 创建 `components/` 下对应文件
3. 原页面改为 import 组件
4. 前端 HMR 验证无报错

---

## 验证

```
P1: npx tsc --noEmit → 0 错误
P2: npx playwright test → 4 新 spec 通过
P3: 前端 localhost:3003 → 页面正常渲染
```

---

## 预估工作量

| 阶段 | 时间 |
|------|------|
| P1 清理 | 10 min |
| P2 E2E   | 30 min |
| P3 组件  | 20 min |
| **合计** | **~1h** |
